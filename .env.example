# Cloudflare Worker URL — set this after deploying the worker
# Example: https://nairawatch-api.YOUR_SUBDOMAIN.workers.dev
VITE_API_URL=

# For local development with wrangler dev (port 8787 is the default)
# Leave VITE_API_URL blank and the Vite proxy will forward /api to localhost:8787
